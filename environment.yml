name: tagged-meshes
channels:
  - conda-forge
  - cadquery
dependencies:
  - python>=3.10,<=3.13
  - cadquery>=2.6.0
  - pytest
  - gmsh
  - python-gmsh
  - appdirs
  - pip
  - pip:
    - --editable=.
